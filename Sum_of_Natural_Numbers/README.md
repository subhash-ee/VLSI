
#Designed and synthesized sum of natural numbers architecture in three different ways:

# 1) sum_no_fsm folder consists of sum of natural numbers architecture with out fsm

# 2) sum_fsm_2 folder have sum of natural numbers architecture with 2-state fsm

# 3) sum_fsm_3 folder contains sum of natural numbers architecture with 3-state fsm

# Every architecture is synthesized, generated gate level netlist, area and timing reports

#sum_no_fsm have corner cases which is highlighted in corner cases png file, to eliminate that corner cases sum_fsm_2 is implemented and sum_fsm_3 is made due to haves one separate done state which is controlled by acknowledgement signal.
